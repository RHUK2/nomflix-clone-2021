import HomeContainer from './HomeContainer';

// import HomeContainer from 'Components/Home';
// 모듈 경로를 폴더로 설정하면 index.js 파일을 먼저 찾기 때문에
// 이와 같이 index.js를 만들어서 export 해주는 것
export default HomeContainer;
